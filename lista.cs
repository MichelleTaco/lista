class ordenadorNumeros
{
    Lista<int>Enteros(1,2,3,5,6,8,0,3,6);
    public Lista<int>ordenar(Lista <int> listaDesordenada)
    {
        while(listaDesordenada.count()>0);
        var listaDesordenada= new Lista<int>();
        var imenor=0
        for (i=0; i<listaDesordenada.count(); i++)
        {
            listaDesordenada[i]<listaDesordenada[imenor]
            imenor=i;
        }
        listaOrdenada.add(listaDesordenada[imenor]);
        listaDesordenada.remove(imenor);
    }
    retun listaOrdenada;
}
